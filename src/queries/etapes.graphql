query getEtapes($locale: SiteLocale) {
  etapes: allEtapes(locale: $locale) {
    coordonnees {
      coordX
      coordY
    }
    dateDeDebut
    dateDeFin
    description(locale: $locale)
    images {
      blurhash
      url
    }
    liens {
      lien
    }
    lieu
    nom
    sousTitre
    off
  }
}